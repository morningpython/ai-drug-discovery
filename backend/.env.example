# 환경변수 설정
# 로컬 개발 환경: .env.local 파일 생성 후 아래 변수 설정

# CORS 설정 (쉼표로 구분된 origin 목록)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# 백엔드 설정
API_VERSION=v0.1.0
ENVIRONMENT=development

# 데이터베이스 (향후 추가)
# DATABASE_URL=

# Azure 설정 (향후 추가)
# AZURE_COSMOS_CONNECTION_STRING=
# AZURE_COSMOS_DATABASE=

# AI 모델 설정 (향후 추가)
# TRANSFORMER_MODEL_PATH=
# GNN_MODEL_PATH=
